// Package session will own workspace locking, metadata and display caches.
// TODO(D): persist the session ID before execution; never use a display cache as
// model history or copy the kernel's private replay implementation.
package session
